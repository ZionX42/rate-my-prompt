import { describe, it, expect } from '@jest/globals';

describe('MongoDB legacy tests', () => {
  it.skip('legacy MongoDB tests are deprecated after Appwrite migration', () => {
    expect(true).toBe(true);
  });
});
